# Blockchain & Docker Setup
## Welcome! This folder contains all the necessary configurations and scripts to get your blockchain environment running inside Docker containers.

### What’s Inside?
In this directory, you will find the complete setups for:

- Blockchain Configurations via **(blockchainSetup.md)**.
- Docker Orchestration (docker-compose.yml and Dockerfiles) via **{dockerSetup.md}**.
